import moment from 'moment';

const bookFuture = async (param) => {
    // return null
}

const orderFuture = async (param) => {
    // config_logging(logging, logging.DEBUG)
    // const key = ""
    // const secret = ""
    // const client = Client(key, secret, base_url="https://fapi.binance.com")

    // try {
    //     const response = client.new_order(symbol="BTCUSDT", side = "SELL", type= "LIMIT", quantity= 0.001, timeInForce="GTC", price= 59808.02)
    // } catch (e) {
    //     console.log("e: ", e);
    // }
    // logging.info(response)
    // except ClientError as error:
    //     logging.error(
    //         "Found error. status: {}, error code: {}, error message: {}".format(
    //             error.status_code, error.error_code, error.error_message
    //         )
    //     )
}
export {
    bookFuture,
}

